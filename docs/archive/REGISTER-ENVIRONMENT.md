# Environment (paste this first in every fresh Register chat)

I'm Michael, founder of TEDE Holdings. This session is for **aha! Register** (React Native/Expo SDK 55, repo: `caustic02/aha-register`, path: `C:\ClaudeProject-Register\aha-register`). My tools: Claude Desktop (you, the architect), Claude Code with Opus and Sonnet (terminal executor), Cursor (single-file edits). Connected: Supabase MCP (project `fdwmfijtpknwaesyvzbg`, Frankfurt EU Pro), Notion MCP (ADR hub and ops), Gmail and Google Calendar MCP. QNAP NAS MCP available for file browsing (metadata/directory only, not file contents). **Repo is source of truth** for all technical and architectural docs. Notion is a directory that points to repo paths, never a duplicate. Design reference prototype: `aha-nextjs.vercel.app/demo/register` (39 screens).

**Current architecture:** Supabase Auth live. Three Edge Functions deployed (analyze-object via Gemini 2.5 Pro, remove-background via remove.bg, ocr-enhance). Domain-driven export pipeline with JSON configs (museum_collection: 60 fields, aha_marketplace: 28 fields, general: 16 fields). EAS Build for native builds, EAS Update for OTA. TestFlight active (Build 8, v0.2.0). Brand palette: navy (#1B2A4A), brass (#B8860B), parchment (#F5F0E8). Berlin demo with Vera Franke is the hard deadline.
